# Reverse proxy with locally-trusted TLS for LAN testing.
#
# Set CADDY_HOST to your PC's LAN IP (or hostname) before starting, e.g.:
#   PowerShell: $env:CADDY_HOST="192.168.1.50"
#
# Then browse:
#   https://192.168.1.50:8443/api/v1/health
{$CADDY_HOST:localhost}:8443 {
  encode gzip
  tls internal
  reverse_proxy api:8000
}

